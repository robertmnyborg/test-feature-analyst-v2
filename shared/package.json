{
  "name": "@feature-analyst/shared",
  "version": "2.0.0",
  "private": true,
  "description": "Shared types, interfaces, and domain models for Feature Analyst V2",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "echo 'No tests in shared package'"
  },
  "devDependencies": {
    "typescript": "^5.2.0"
  }
}
